---
import BaseHead from '../components/BaseHead.astro'
import Header from '../components/Header.astro'
import Footer from '../components/Footer.astro'
import { SITE_TITLE, SITE_DESCRIPTION } from '../config'
import Body from '../components/Body.astro'
import Content from '../components/Content.astro'
import { HOMEPAGE_URL } from '../config'
import Masthead from '@components/Masthead.astro'
import { IoArrowForward } from 'react-icons/io5/index.js'
import { Image } from '@astrojs/image/components'

const posts = (await Astro.glob('./posts/*.md')).sort(
  (a: any, b: any) =>
    new Date(b.frontmatter.createdAt).valueOf() -
    new Date(a.frontmatter.createdAt).valueOf()
)
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
  </head>
  <Body>
    <Header title={SITE_TITLE} />
    <!--
      pt-[56px] 表示 padding-top: 56px;
    -->
    <main class="pt-[56px]">
      <Masthead />
      <Content>
        <section>
          <!--
            mb-8 表示 margin-bottom: 2rem;
            indent-4 表示 text-indent: 1rem;
           -->
          <p class="mb-8 indent-8">
            我是一个Java开发工程师，目前在一家互联网公司从事后端开发的工作。
            我现在主要的开发工具为 MacBook Air M1，主要使用 Jetbrains 的 IDE
            进行开发，喜欢Vim 与 Linux。
            目前计划并希望自己能在开源上有所贡献，正在努力阅读社区优秀的开源项目，
            想能有所成长与突破，希望自己可以坚持下去。
          </p>
          <!--
            inline-flex: 行内元素，但是可以设置宽高
            items-center: 交叉轴居中
            gap-1: 间距为1rem
            indent-1: 缩进1rem
            inline: 行内元素
           -->
          <a
            class="mb-8 inline-flex items-center gap-1 indent-1 text-orange-500"
            href={HOMEPAGE_URL}
          >
            了解更多关于我
            <IoArrowForward className="inline" />
          </a>
        </section>
        <section>
          <ul class="grid grid-cols-1 md:grid-cols-2 gap-4">
            {
              posts.map(post => (
                <li class="text-center mb-4">
                  <a href={post.url}>
                    <Image
                      class="border border-slate-300 dark:border-zinc-700 rounded-xl"
                      src={post.frontmatter.heroImage}
                      width={720 * 2}
                      aspectRatio={2.0}
                      alt="Thumbnail"
                      format={post.frontmatter.iamgeFormat ?? 'avif'}
                    />
                    <div class="mt-3 text-xl font-bold">
                      {post.frontmatter.title}
                    </div>
                    {post.frontmatter.description && (
                      <div class="opacity-70">
                        {post.frontmatter.description}
                      </div>
                    )}
                  </a>
                </li>
              ))
            }
          </ul>
        </section>
      </Content>
    </main>
    <Footer />
  </Body>
</html>
